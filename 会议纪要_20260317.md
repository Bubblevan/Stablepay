# StablePay 项目进度对齐会议纪要

**会议时间**: 2026年03月17日 16:00-16:30
**会议地点**: 线上/线下
**记录人**: 包博文

---

## 1. 会议参与人员

- **@包博文** (Payment Service + API Gateway)
- **@贾越** (Blockchain Adapter)
- **@杨世博** (Verification Service)
- **@Jun Tan** (导师/架构)

---

## 2. 当前项目状态总览

### 2.1 系统架构全景

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                              客户端层                                        │
│  ┌─────────────────┐  ┌─────────────────┐  ┌─────────────────┐              │
│  │  StablePay Skill │  │  开发者后端服务   │  │  StablePay网站  │              │
│  │  (在OpenClaw中)  │  │                 │  │                 │              │
│  └────────┬────────┘  └────────┬────────┘  └────────┬────────┘              │
└───────────┼────────────────────┼────────────────────┼──────────────────────┘
            │                    │                    │
            ▼                    ▼                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                          阿里云 API 网关 (外部云产品)                          │
└─────────────────────────────────┬───────────────────────────────────────────┘
                                  │
                                  ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│                           API Gateway (8080) 【待联调】                        │
└────────────────┬────────────────┬────────────────┬──────────────────────────┘
                 │                │                │
        ┌────────▼────────┐      │         ┌──────▼───────┐
        │   DID Service   │      │         │Query Service │
        │    (8081)       │      │         │   (8085)     │
        │   【🔴 未开始】  │      │         │ 【🔴 未开始】 │
        └─────────────────┘      │         └──────────────┘
                                 │
        ┌────────▼────────┐      │         ┌──────▼───────┐
        │Payment Service  │◄─────┘         │ Verification │
        │    (8082)       │  HTTP 402       │   (8084)     │
        │  【🟡 进行中】   │                │ 【🟢 核心完成】│
        └────────┬────────┘                └──────────────┘
                 │
                 ▼ RPC调用
        ┌───────────────────────┐
        │  Blockchain Adapter   │
        │       (8083)          │
        │    【🟡 进行中】       │
        └───────────┬───────────┘
                    │ Solana网络请求
                    ▼
        ┌───────────────────────┐
        │      Solana 主网      │
        │  【外部区块链网络】      │
        └───────────────────────┘

图例: 🟢 核心完成 | 🟡 进行中/待完善 | 🔴 未开始
```

### 2.2 服务依赖关系

| 调用方 | 被调用方 | 调用方式 | 依赖强度 | 状态 |
|--------|----------|----------|----------|------|
| api-gateway | did-service | HTTP | 必须 | 🔴 阻塞 |
| api-gateway | payment-service | HTTP | 必须 | 🟡 待联调 |
| api-gateway | verification-service | HTTP | 必须 | 🟢 可用 |
| api-gateway | query-service | HTTP | 必须 | 🔴 阻塞 |
| **payment-service** | **blockchain-adapter** | **RPC** | **强依赖** | **🟡 关键路径** |
| **payment-service** | **verification-service** | **MQ** | **强依赖** | **🟡 关键路径** |
| query-service | blockchain-adapter | RPC | 必须 | 🔴 阻塞 |

---

## 3. 各服务进度详情

### 3.1 Payment Service (@包博文) - 进行中

**已完成**:
- [x] Kitex RPC 框架代码生成
- [x] Codex 业务逻辑迁移（支付状态机、幂等性校验）
- [x] Handler 实现（InitiatePayment/GetPaymentStatus/ListPaymentHistory）
- [x] 测试覆盖分析报告

**进行中**:
- [ ] 接入 blockchain-adapter 的转账接口（等待对方完成）
- [ ] 定义 MQ 事件格式（与 verification-service 对齐）
- [ ] Repository 层单元测试

**阻塞点**: 依赖 blockchain-adapter 的 TransferStableCoin 接口

---

### 3.2 Blockchain Adapter (@贾越) - 进行中

**已完成**:
- [x] Solana Client 封装（支持 DevNet/LocalNet/MainNet）
- [x] Gas 费补贴（FeePayer）双签逻辑
- [x] 热钱包管理模块
- [x] Service 层结构（Transfer/Balance/TxStatus）
- [x] Kitex RPC 框架代码生成

**待完善**:
- [ ] Handler 层整合（当前 `handler.go` 为空，需整合 `internal/handler/`）
- [ ] `main.go` 服务启动逻辑（当前只有 `package main`）

**DDL**: 今日（3.17）18:00 前完成 Handler 整合，确保 RPC 接口可调用

---

### 3.3 Verification Service (@杨世博) - 核心功能完成

**已完成**:
- [x] VerifyPurchase 接口实现（已跑通端到端测试）
- [x] MQ Consumer 骨架（监听 payment_events）
- [x] SQLite 数据库集成（本地测试通过）
- [x] Kitex RPC 服务启动

**待完善**:
- [ ] BatchVerifyPurchase 接口（空实现）
- [ ] GetPurchaseProof 接口（空实现）
- [ ] 数据库切 MySQL（联调前）

**DDL**: 今日（3.17）20:00 前补全剩余两个接口

---

### 3.4 DID Service - 🔴 未开始

**状态**: 暂无进展

**需求**:
- DID 生成（W3C did:solana 标准）
- 身份验证接口
- 钱包注册

**建议负责人**: 待分配（或询问现有成员是否可兼任）

---

### 3.5 Query Service - 🔴 未开始

**状态**: 暂无进展

**需求**:
- 余额查询（调用 blockchain-adapter）
- 交易记录查询
- 收益统计

**建议负责人**: 待分配

---

## 4. 关键技术规范

### 4.1 Kitex/Thrift 代码生成命令

所有服务需要执行以下命令生成 Kitex 代码：

```bash
# 1. 进入服务目录
cd D:\MyLab\StablePay\{service-name}

# 2. 生成 Kitex 服务端代码（带 -service 参数）
kitex -service {service-name} \
  -module github.com/stablepay/{service-name} \
  -I ../stablepayai-idl/idl \
  ../stablepayai-idl/idl/{service-name}.thrift

# 示例：
# did-service
kitex -service did-service \
  -module github.com/stablepay/did-service \
  -I ../stablepayai-idl/idl \
  ../stablepayai-idl/idl/did-service.thrift

# query-service
kitex -service query-service \
  -module github.com/stablepay/query-service \
  -I ../stablepayai-idl/idl \
  ../stablepayai-idl/idl/query-service.thrift
```

**api-gateway 只需要生成客户端**（不带 `-service` 参数）：
```bash
cd D:\MyLab\StablePay\api-gateway

# 生成 common（基础类型）
kitex -module stablepay/api-gateway \
  -I ../stablepayai-idl/idl \
  ../stablepayai-idl/idl/common.thrift

# 逐个生成服务客户端
kitex -module stablepay/api-gateway \
  -I ../stablepayai-idl/idl \
  ../stablepayai-idl/idl/did-service.thrift

kitex -module stablepay/api-gateway \
  -I ../stablepayai-idl/idl \
  ../stablepayai-idl/idl/payment-service.thrift

kitex -module stablepay/api-gateway \
  -I ../stablepayai-idl/idl \
  ../stablepayai-idl/idl/blockchain-adapter.thrift

kitex -module stablepay/api-gateway \
  -I ../stablepayai-idl/idl \
  ../stablepayai-idl/idl/verification-service.thrift

kitex -module stablepay/api-gateway \
  -I ../stablepayai-idl/idl \
  ../stablepayai-idl/idl/query-service.thrift
```

### 4.2 stablepay-common 依赖说明

**必须接入**，所有服务都需要：

```bash
# 添加依赖
go get code.wenfu.cn/stablepayai/stablepay-common@master
go mod tidy
```

**强制使用规范**:

| 场景 | 使用包 | 禁止 |
|------|--------|------|
| 错误码 | `constants` 包 | 自己定义 error code |
| 金额计算 | `money` / `chainmoney` | float64 |
| ID 生成 | `id_generator` | UUID |
| 日志 | `log` | 标准库 log |

示例：
```go
import (
    "code.wenfu.cn/stablepayai/stablepay-common/constants"
    "code.wenfu.cn/stablepayai/stablepay-common/money"
)

// ✅ 正确
return &constants.BusinessErrorCode{
    Code: constants.BusinessErrorCodeInsufficientBalance,
    Message: "余额不足",
}

// ✅ 正确
amount, _ := money.NewFromString("10.50", "USDC")
amountMinor := amount.MinorUnit() // 链上用

// ❌ 错误
return errors.New("余额不足") // 不要自己造错误
```

详细文档见: `stablepay-common-master/USAGE_GUIDE.md`

---

## 5. 推广联调策略

### 5.1 第一阶段：核心链路联调（3.17-3.18）

**目标**: 跑通 `支付 → 区块链 → MQ → 验证` 完整链路

```
Payment Service (包博文)
    ↓ RPC 调用
Blockchain Adapter (贾越)
    ↓ 转账成功
Payment Service 发 MQ
    ↓ 消费
Verification Service (杨世博)
```

**联调步骤**:
1. 18:00 前：贾越完成 blockchain-adapter Handler 整合
2. 20:00 前：杨世博补全 verification 剩余接口
3. 21:00：三方联调，包博文发起支付请求
4. 验证点：
   - RPC 调用成功（TransferStableCoin）
   - Solana 链上交易成功（DevNet）
   - MQ 消息发送成功
   - Verification 消费并入库成功

### 5.2 第二阶段：网关接入联调（3.19）

**目标**: api-gateway 接入已就绪的服务

```
api-gateway
    ├──► payment-service (接入)
    ├──► verification-service (接入)
    └──► blockchain-adapter (不直接暴露，内部调用)
```

### 5.3 第三阶段：补齐缺失服务（3.20-3.21）

**目标**: 启动 DID Service 和 Query Service 开发

**方案 A（推荐）**: 现有成员兼任
- Query Service 可由包博文或贾越兼任（逻辑简单，主要是透传）
- DID Service 可由杨世博或贾越兼任

**方案 B**: 申请新增人员
- 如时间紧张，向老板申请增派人手

---

## 6. 待办任务（DDL 明确）

### 今日（3.17）DDL

| 任务 | 负责人 | DDL | 阻塞点 |
|------|--------|-----|--------|
| blockchain-adapter Handler 整合 | 贾越 | 18:00 | 无 |
| verification 补全剩余接口 | 杨世博 | 20:00 | 无 |
| 三方联调（支付→区块链→MQ→验证） | 全员 | 21:00 | 依赖上面两个任务 |
| MQ 事件格式文档 | 包博文 | 18:00 | 无 |

### 明日（3.18）DDL

| 任务 | 负责人 | DDL | 备注 |
|------|--------|-----|------|
| Payment Service 接入 blockchain-adapter | 包博文 | 12:00 | 等今晚接口 ready |
| Payment Service 接入 MQ | 包博文 | 18:00 | 发送支付事件 |
| api-gateway 接入 payment/verification | 包博文 | 20:00 | Mock → RPC |
| 第一轮端到端测试 | 全员 | 21:00 | curl 测试完整链路 |

### 本周（3.17-3.21）DDL

| 任务 | 负责人 | DDL | 状态 |
|------|--------|-----|------|
| DID Service 启动开发 | 待分配 | 3.20 | 🔴 未分配 |
| Query Service 启动开发 | 待分配 | 3.21 | 🔴 未分配 |
| 全链路联调通过 | 全员 | 3.21 | 🟡 依赖 DID/Query |

---

## 7. 需要老板决策的事项

### 7.1 人员分配

**问题**: DID Service 和 Query Service 尚未启动，是否需要：

- [ ] **方案 A**: 现有 3 人兼任（建议 Query 给包博文，DID 给杨世博）
- [ ] **方案 B**: 申请增派 1-2 名同学
- [ ] **方案 C**: 延后 DID/Query 到二期（先用 Mock 挡板）

### 7.2 基础设施

**问题**: 联调环境准备

- [ ] **MQ 选型**: 本地 Docker RocketMQ vs 阿里云 RocketMQ
- [ ] **Solana 网络**: DevNet（免费但慢）vs Local Validator（快但需配置）
- [ ] **数据库**: 各服务本地 MySQL vs 统一阿里云 RDS

### 7.3 Codeup 仓库权限

**问题**: 部分同学可能尚未配置 Codeup 访问权限，需要统一申请

---

## 8. 会议结论

1. **今日关键路径**: blockchain-adapter Handler 整合 → 三方联调
2. **本周目标**: 核心链路（支付+区块链+验证）跑通，补齐 DID/Query
3. **风险点**: DID/Query 尚未分配，可能成为 3.21 联调瓶颈
4. **下一步**: 今晚 21:00 确认三方联调结果，明天启动网关接入

---

## 9. 附录：本地快速测试命令

```bash
# 1. 启动所有服务（6个终端）
cd blockchain-adapter && go run cmd/server/main.go    # 8083
cd did-service && go run main.go                      # 8081（待启动）
cd payment-service && go run main.go handler.go       # 8082
cd verification && go run main.go                     # 8084
cd query-service && go run main.go                    # 8085（待启动）
cd api-gateway && go run cmd/api-gateway/main.go      # 8080（待联调）

# 2. 测试支付（HTTP 402 流程）
curl -X POST http://localhost:8080/api/v1/pay \
  -H "Content-Type: application/json" \
  -H "X-DID: did:solana:abc123" \
  -d '{
    "agent_did": "did:solana:agent1",
    "skill_did": "did:solana:skill1",
    "amount": "5.00",
    "currency": "USDC"
  }'

# 3. 验证购买记录
curl "http://localhost:8080/api/v1/verify?agent_did=xxx&skill_did=yyy"
```

---

**下次会议**: 2026年03月18日 16:00（每日站会）
