# StablePay Merchant Server 配置
# 路径: config/config.yaml

server:
  # 监听地址
  host: "0.0.0.0"
  port: 8787
  # 服务名称 (用于日志/可观测性)
  name: "stablepay-merchant-server"

# StablePay API Gateway 配置
stablepay:
  # API Gateway 基础地址 (生产环境)
  gateway_base_url: "https://ai.wenfu.cn"
  # 内部 API Key (商户后端与 Gateway 通信使用)
  api_key: "stablepay-dev-key"
  # Facilitator 地址 (x402 标准支付中介)
  facilitator_url: "https://ai.wenfu.cn"

# 商户钱包配置
merchant:
  # 收款钱包地址 (Solana)
  seller_address: "2kZGwkLnVdSxjjNueeUQmqBf3tRKMn7y1bbktRZkJWdR"
  # 生成访问 proof 的密钥 (仅后端内部使用，不上传到客户端)
  proof_secret: "replace-this-with-a-long-random-secret"
  # 商户服务公网地址，用于 x402 v2 resource.url；本地开发可用 localhost
  public_base_url: "http://127.0.0.1:8787"

# 数据库配置
database:
  # 驱动类型: "sqlite"（本地开发）或 "mysql"（生产环境）
  driver: "mysql"
  # SQLite 文件路径（driver=sqlite 时使用）
  path: "./data/merchant.db"
  # 是否在启动时自动迁移 (建表/种子数据)
  auto_migrate: true
  # 是否写入种子数据 (首次启动时)
  seed_enabled: true
  # MySQL 连接参数（driver=mysql 时使用，密码优先从环境变量 MYSQL_PASSWORD 读取）
  mysql_host: "stablepay-mysql"
  mysql_port: 3306
  mysql_user: "stablepay"
  mysql_password: "stablepay123"
  mysql_db_name: "stablepay_merchant_db"

# 区块链配置
blockchain:
  solana_network: "solana:5eykt4UsFv8P8NJdTREpY1vzqKqZKvdp"
  usdc_mint: "EPjFWdd5AufqSSqeM2qN1xzybapC8G4wEGGkZwyTDt1v"
