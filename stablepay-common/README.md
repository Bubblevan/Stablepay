# stablepay-common

Non-communication shared libraries only: logging, tracing, ID generation, money and related primitives.

Service RPC/API contracts do not belong here; those are owned by `stablepayai-idl` and generated into each service's `kitex_gen/`.
