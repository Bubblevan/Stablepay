我是 StablePay DEMO 的研发人员，目前我已经完成了 StablePay DEMO 的 PRD 设计文档和技术方案文档。

请基于以下 PRD 文档和技术方案文档，产出 StablePay 的单个服务代码仓库：{REPO_NAME}
当前仓库仅对应一个服务：Query Service

PRD 文档：{PRD_LINK}
技术方案文档：{TECH_LINK}

说明：
- 如果 larksuite / 飞书链接需要 MCP，请使用 MCP 访问
- 如果任一链接无法访问、内容不一致、或信息缺失，请先停止并明确指出问题，等待我确认后再继续
- 只生成当前这个服务的代码仓库，不要生成整个 StablePay
- 代码注释使用中文
- 必须包含完整单元测试、运行手册、运维手册、模块说明文档、对外接口说明
- 先详细列出技术选型、模块设计、接口设计、依赖关系和目录结构，等我确认后再生成代码

【当前服务】
服务名称：Query Service
核心职责：
- 余额查询
- 交易记录查询
- 收益统计查询
- 聚合查询
- 读性能优化
- 缓存管理

【技术要求】
- 技术选型必须与 StablePay DEMO 保持一致，优先遵循技术方案文档
- 使用 Go 语言
- 使用 CloudWeGo 生态
- 仓库结构参考 COLA v5 分层思想
- 对外提供查询接口，支持用户侧与开发者侧的查询需求
- 内部允许依赖 Blockchain Adapter 查询链上余额 / 状态
- 要体现缓存策略与读优化能力，优先使用 Redis
- 要考虑后续统计扩展、分页、筛选、时间范围查询
- 要考虑读模型与业务模型分离（如果技术方案适合）

【必须实现的能力】
1. 余额查询
2. 用户交易记录查询
3. 开发者收益统计查询
4. 开发者销售记录查询（若文档要求）
5. 交易分页、筛选、排序
6. Redis 缓存
7. 对 Blockchain Adapter 的查询调用
8. 单元测试：
   - 缓存命中 / 未命中
   - 分页查询
   - 收益统计
   - 时间筛选
   - 异常参数
   - 下游依赖失败场景

【特别要求】
- 查询接口设计要区分：
  - 用户侧（余额 / 历史购买记录）
  - 开发者侧（收益 / 销售记录）
- 如果文档要求支持“本月 / 上月 / 全部”等筛选，请纳入设计
- 如果技术方案提到 CQRS / 冗余读模型，请先分析是否要在 DEMO 阶段实现，给出建议后等我确认
- 需要显式设计缓存 key、TTL、失效策略

【输出顺序要求】
第一步：先输出，不要写代码
1. 需求摘要
2. 领域模型与查询模型设计
3. 技术选型清单
4. 数据库 / 读模型设计
5. 接口设计
6. 缓存设计
7. 下游依赖设计（Blockchain Adapter 等）
8. 测试策略
9. 运行与部署说明提纲
10. 文档冲突点 / 待确认问题

第二步：在我确认后，再生成完整代码仓库，要求包含：
- 完整 Go 代码
- 配置文件
- 数据库脚本
- Redis 缓存逻辑
- 单元测试
- README
- 运维手册
- 对外接口说明

检查：
- 只生成 Query Service 仓库
- 不要生成其他服务代码
- 若文档有冲突，先列出问题