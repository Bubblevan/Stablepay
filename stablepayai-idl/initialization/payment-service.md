我是 StablePay DEMO 的研发人员，目前我已经完成了 StablePay DEMO 的 PRD 设计文档和技术方案文档。

请基于以下 PRD 文档和技术方案文档，产出 StablePay 的单个服务代码仓库：{REPO_NAME}
当前仓库仅对应一个服务：Payment Service

PRD 文档：{PRD_LINK}
技术方案文档：{TECH_LINK}

说明：
- 如果 larksuite / 飞书链接需要 MCP，请使用 MCP 访问
- 如果任一链接无法访问、内容不一致、或信息缺失，请先停止并明确指出问题，等待我确认后再继续
- 只生成当前这个服务的代码仓库，不要生成整个 StablePay
- 代码注释使用中文
- 必须包含完整单元测试、运行手册、运维手册、模块说明文档、对外接口说明
- 先详细列出技术选型、模块设计、接口设计、依赖关系和目录结构，等我确认后再生成代码

【当前服务】
服务名称：Payment Service
核心职责：
- HTTP 402 协议处理
- 支付流程控制
- 签名验证
- 链上交易协调
- 支付状态管理
- 支付历史查询
- 发布支付成功 / 失败事件供其他服务消费

【技术要求】
- 技术选型必须与 StablePay DEMO 保持一致，优先遵循技术方案文档
- 使用 Go 语言
- 使用 CloudWeGo 生态
- 对外接口按技术方案实现：发起支付、查询支付状态、支付历史等
- 内部需依赖 DID Service（身份 / 签名校验）与 Blockchain Adapter（链上执行）
- 需要对接 RocketMQ（如技术方案有异步事件要求）
- 仓库结构参考 COLA v5 分层思想
- 需特别关注：
  - 幂等性
  - 重试与补偿
  - 支付状态机
  - 交易超时
  - 请求签名校验
  - 重放攻击防护
  - 错误码设计

【必须实现的能力】
1. HTTP 402 Payment Required 流程处理
2. 发起支付接口
3. 查询支付状态接口
4. 查询支付历史接口
5. 调用 DID Service 校验身份与签名
6. 调用 Blockchain Adapter 执行链上交易
7. 记录支付交易与状态
8. 发布支付成功 / 失败事件
9. 单元测试与业务流程测试：
   - 正常支付
   - 重复支付
   - 余额不足
   - 签名失败
   - 链上执行失败
   - 超时重试
   - 消息发布

【特别要求】
- 支付领域模型必须清晰设计：Payment、Status、TxID、TxHash、Currency、Agent DID、Skill DID 等
- 如果 PRD 与技术方案对支付接口定义、状态码、响应格式有差异，请先列出
- 需要先给出支付状态机设计图或文字说明
- 需要说明同步链上执行与异步事件更新之间如何处理一致性

【输出顺序要求】
第一步：先输出，不要写代码
1. 需求摘要
2. 支付领域模型设计
3. 支付状态机设计
4. 技术选型清单
5. 数据库设计
6. 接口设计
7. 下游依赖设计（DID / Blockchain / MQ）
8. 幂等与补偿设计
9. 测试策略
10. 运行与部署说明提纲
11. 文档冲突点 / 待确认问题

第二步：在我确认后，再生成完整代码仓库，要求包含：
- 完整 Go 代码
- 配置文件
- 数据库脚本
- RocketMQ 相关代码
- 单元测试
- README
- 运维手册
- 对外接口说明

检查：
- 只生成 Payment Service 仓库
- 不要生成其他服务代码
- 若发现文档不一致，先停下来列出问题