我是 StablePay DEMO 的研发人员，目前我已经完成了 StablePay DEMO 的 PRD 设计文档和技术方案文档。

请基于以下 PRD 文档和技术方案文档，产出 StablePay 的单个服务代码仓库：{REPO_NAME}
当前仓库仅对应一个服务：DID Service

PRD 文档：{PRD_LINK}
技术方案文档：{TECH_LINK}

说明：
- 如果 larksuite / 飞书链接需要 MCP，请使用 MCP 访问
- 如果任一链接无法访问、内容不一致、或信息缺失，请先停止并明确指出问题，等待我确认后再继续
- 只生成当前这个服务的代码仓库，不要生成整个 StablePay
- 代码注释使用中文
- 必须包含完整单元测试、运行手册、运维手册、模块说明文档、对外接口说明
- 先详细列出技术选型、模块设计、接口设计、依赖关系和目录结构，等我确认后再生成代码

【当前服务】
服务名称：DID Service
核心职责：
- W3C did:solana 身份管理
- DID 生成
- 身份验证
- 钱包注册
- 签名校验
- 配置管理（如钱包配置、支付偏好等，以技术方案为准）

【技术要求】
- 技术选型必须与 StablePay DEMO 保持一致，优先遵循技术方案文档
- 使用 Go 语言
- 使用 CloudWeGo 生态
- 对外接口以技术方案为准：优先实现 DID 创建、查询、签名验证、配置更新相关接口
- 仓库结构参考 COLA v5 分层思想
- 单仓库模式；如需 go work / 多模块工作区，请先说明理由
- 注重安全性：密钥生成、签名验证、配置加密、参数校验、状态校验
- 要支持后续被 Payment Service 通过 RPC 或服务调用方式依赖
- 必须考虑 DID 唯一性校验、状态管理、防重放相关设计（若文档有时间戳/nonce 机制，以文档为准）

【必须实现的能力】
1. DID 创建
   - 生成 did:solana 标识
   - 生成对应钱包身份信息
2. DID 查询
3. DID 签名验证
4. DID 状态管理（active / disabled 等，以文档为准）
5. DID 配置管理
6. 对外服务接口（HTTP 或 RPC 以技术方案为准）
7. 适当的数据持久化层设计
8. 完整单元测试：
   - DID 生成
   - 签名验证
   - 异常输入
   - 状态变更
   - 配置更新

【特别要求】
- 如果 PRD 与技术方案对 DID 数据库存储存在冲突（例如 MySQL / MongoDB 描述不一致），请先列出冲突并给出你的建议，不要直接拍脑袋实现
- 如果需要钱包密钥相关内容，请明确区分：
  - 服务端持有什么
  - 客户端本地持有什么
  - 哪些内容不能存储

【输出顺序要求】
第一步：先输出，不要写代码
1. 你从 PRD / 技术方案中提取到的 DID Service 需求摘要
2. 领域模型设计（DID、Config、Status、Signature Verification）
3. 技术选型清单
4. 数据库表/集合设计
5. 接口设计
6. 安全设计
7. 测试策略
8. 运行与部署说明提纲
9. 文档冲突点 / 待确认问题

第二步：在我确认后，再生成完整代码仓库，要求包含：
- 完整 Go 代码
- 配置文件
- 建表脚本 / 初始化脚本（如适用）
- 单元测试
- README
- 运维手册
- 对外接口说明

检查：
- 只生成 DID Service 仓库
- 不要生成其他服务代码
- 如果文档存在冲突，先列出等待确认